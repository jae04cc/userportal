node_modules
.next
data
.git
.github
*.db
*.db-*
.env
.env.local
.env.*.local
# scripts/reset-admin.mjs is deliberately NOT excluded — it's the documented
# lockout recovery path and has to exist inside the running container.
scripts/seed-test-data.mjs
README.md
